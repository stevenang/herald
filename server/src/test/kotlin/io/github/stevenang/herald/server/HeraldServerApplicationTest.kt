package io.github.stevenang.herald.server

import org.junit.jupiter.api.Test
import org.springframework.boot.test.context.SpringBootTest

@SpringBootTest
class HeraldServerApplicationTest {

    @Test
    fun `application context loads`() {
        // Intentionally empty — fails the build if Spring wiring is broken.
    }
}